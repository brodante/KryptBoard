{
  "name": "kryptboard-webextension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KryptBoard browser extension — encrypted keyboard overlay that mirrors the Android IME's envelope format.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:dom": "node --test tests/dom.test.mjs",
    "test:crypto": "node --test tests/crypto.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
